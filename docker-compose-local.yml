services:
  kitchenowl:
    image: kitchenowl:local-loyalty
    restart: unless-stopped
    ports:
      - "80:8080"
    environment:
      - JWT_SECRET_KEY=PLEASE_CHANGE_ME_TO_A_SECURE_KEY
    volumes:
      - kitchenowl_data:/data

volumes:
  kitchenowl_data:


